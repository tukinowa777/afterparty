# Development Notes

## 2026-03-30

- Git 管理を開始し、初回コミットを作成した
- 店舗データを `data/venues.json` に分離した
- `GET /api/venues` を追加し、静的配信から簡易 API 配信へ拡張した
- 共有 URL と外部地図導線を追加した
- 条件付き検索とスコアリングをサーバー側で返す構成へ移行した
- OpenStreetMap / Overpass の無料データ取得を追加し、失敗時はサンプルデータへフォールバックするようにした
- 銀座線、丸ノ内線、山手線、田園都市線の駅一覧を追加し、GPSと駅指定を切り替えられるようにした
- 駅ジオコーディング結果を `data/station_coordinates.json` に保存するようにした
- アプリ自身で自己署名HTTPS待受できるようにし、`scripts/start_https.sh` を追加した
- `scripts/run_public_https.sh` と `scripts/stop_public_https.sh` を追加し、8443のHTTPS待受を起動しやすくした
- ホットペッパー検索の駅名デコードを修正し、失敗時はサンプルではなく空結果を返すようにした
- 距離条件を `500m以内` と `1km以内` の2択に整理した
- カラオケ店を候補から除外しないように調整した
- 人数項目をUIと検索条件から削除した
- 路線と駅の不整合がある場合は正しい路線へ補正して検索起点を解決するようにした
- 駅ジオコーディングが失敗またはレート制限された場合でも空応答にならずJSONを返すようにした
- `新宿` の駅座標キャッシュを追加して主要検索が安定するようにした
- 駅検索の徒歩圏条件を `徒歩10分以内` に固定し、徒歩時間ベースで候補を絞るようにした
- 駅検索ではホットペッパーの `access` / `station_name` に指定駅から徒歩10分以内と読める店だけを対象にするよう切り替えた
- 距離選択UIを削除し、駅検索は上記条件をデフォルトで使うようにした
- 駅検索ではホットペッパー店舗ページの `クーポン・地図` タブにある `道案内` を取得し、その記載から徒歩10分以内判定を行うようにした
- `直通`、`駅すぐ`、複数駅表記、主要駅の周辺駅エイリアスも拾うように `道案内` 判定を強化した
- 和風居酒屋、居酒屋チェーン、バー、焼き鳥が上位に来やすいようにスコアリングを調整した
- 検索結果は3件ずつの横スクロールページで見られるようにした
- 結果カードに喫煙 / 禁煙 / 分煙 / 要確認 を表示するようにした
- 全駅共通で、駅検索が 0 件のときはホットペッパー取得条件を広げて再検索するようにした
- 駅徒歩判定は指定駅名と手動エイリアスだけを見るように見直し、`新宿` と `新宿御苑前` のような前方一致の誤判定を防いだ
- 修正後の確認として、`渋谷` は 30 件、`新宿` は 50 件、`飯田橋` は 1 件、`下北沢` は 2 件の候補が返ることを確認した
- 駅検索は緯度経度中心をやめ、ホットペッパーの駅名キーワード検索を共通ロジックとして使う形へ切り替えた
- 現在地検索UIを削除し、画面は `条件を設定` から始まる駅検索専用フローへ変更した
- 駅名キーワード化後の再確認として、`渋谷` は 39 件、`新宿` は 54 件、`飯田橋` は 70 件、`下北沢` は 66 件、`成城学園前` は 5 件の候補が返ることを確認した
- 結果カードをコンパクト化し、3件が一画面に収まりやすいよう余白と文字サイズを調整した
- 結果件数表示を `1-3件目 / 全N件` の形式に変え、`次の3件` が分かりやすいページャー表示にした
- 検索結果は料理カテゴリごとにページ分けし、`焼き鳥 / 海鮮 / 和食` などのカテゴリが分かるようにした
- ホットペッパー駅名検索の総件数は断定表示せず、`候補を表示しています` の案内文に変更した
- 検索条件に `喫煙条件` を追加し、`禁煙` と `喫煙可` でホットペッパーの喫煙情報を使って絞り込めるようにした
- `喫煙可` 選択時は `分煙` も含めるように喫煙条件の判定を調整した
- 喫煙情報はホットペッパーAPIの項目だけでなく、各店舗トップページの `たばこ` セクションから `禁煙・喫煙` と `喫煙専用室` を取得して補正するようにした
- 料理ジャンルを `和食 / 中華 / 洋食 / 焼き鳥 / 焼肉 / 韓国料理 / 創作` の7分類に整理し、UIとホットペッパー分類ロジックを揃えた
- 旧フィルタで `中華 / 韓国料理 / 焼肉 / 洋食` を入口除外していたため0件になる問題を修正し、店名に `やきとり / 焼鳥` を含む店も `焼き鳥` に再分類するようにした
- フロント側のカテゴリ見出しも新旧キーを正規化し、特定ジャンル検索時に `その他` と表示されないようにした
- ブラウザキャッシュで旧 `app.js` が残るケースに備えてアセットURLへバージョンを付け、価格表示の `¥` は `3千円前後 / 5千円前後 / 7千円前後` の表記に変更した
- 結果見出しの `おすすめ3件` を `検索結果` に変更し、スマートフォン幅では店舗カード上部を2段グリッドにして店名や価格表示が崩れにくいように調整した
- 長い店名や駅案内、営業時間ラベルでカードが縦に伸びすぎないように、店名は2行まで、駅案内とメタ情報は1行省略、スマホではメタ情報を2列グリッド化する調整を入れた
- カード高さのばらつきをさらに抑えるため、特徴文は1項目に絞り、ジャンル・ボタンも1行省略、アクション領域は常に2列グリッドに統一した
- カテゴリラベルやピルが列幅いっぱいに伸びて余白が大きく見える問題に対して、内容幅だけを使うよう `fit-content` と `justify-self` を指定した
- ジャンル表示はテキスト行ではなく個別のチップ表示に変更し、`韓国料理` など短いラベルでも前後に大きな空白が見えないようにした
- ジャンル行や特徴行が空でも高さを取らないように、空要素は描画しないよう修正し、カード内の行間もさらに詰めた
- カテゴリ見出しと1件目カードの間、カード末尾のボタン下余白が目立たないように、ページ間隔・カード内 `gap`・ボタン周りの余白とパディングを全体的に圧縮した
- README にシステム仕様を追加し、使用言語、標準ライブラリ、外部サービス、現在のデータソース構成を明記した
- 飯田橋の検索結果で `韓国料理` が1件しかないカテゴリでも大きな空白が出る件は、API件数ではなく、横スクロール親の `flex` とカテゴリページの `grid` が最も高いページに引き伸ばされていたのが原因で、`align-items: flex-start` と `align-content: start` で内容高さに合わせるよう修正した
- 検索結果ページ左上の料理ジャンル見出しは不要になったため削除し、料理カテゴリは各カード内のジャンルチップだけで表現するよう整理した
- 結果ページは料理カテゴリ単位で区切らず、配列順のまま3件ずつ連続表示する形に戻し、`韓国料理1件だけのページ` のような分断を解消した
- 結果カード右上の `3千円前後 / 5千円前後 / 7千円前後` 表示は不要になったため削除した
- `3次会チェック` を追加し、ON のときは `22:00以降も入れる店` だけに絞り込めるようにした
- 駅検索のホットペッパー取得では `居酒屋` 系 genre 固定をやめ、料理ジャンルはアプリ側で絞るようにした
- 駅検索キーワードは `指定駅 + 手動エイリアス + 同一路線の隣接駅` を全駅共通で使うようにし、周辺駅名で登録された店も拾えるようにした
- 外苑前と青山一丁目の `中華` は公開APIで 0 件だったが、修正後はホットペッパーから外苑前 11 件、青山一丁目 12 件返ることを確認した
- `2次会チェック` と `3次会チェック` は閉店時刻ではなく `LO時刻` で判定するように修正した
- `3次会チェック` では `22:00以降にLO可能`、`2次会チェック` では `21:00以降にLO可能` な店だけを残すようにし、`日比谷園 青山店` のような 21:00 LO の店は 3次会結果から除外されることを確認した
- 画面で古い `app.js` / `styles.css` を掴む可能性があったため、HTML / CSS / JS には `Cache-Control: no-store` を付け、アセットURLの版も更新した
- 公開API `openAfter22=true` の確認では `日比谷園 青山店` は返らず、`中国茶房8 青山店` など `22:00以降にLO可能` な店のみ返ることを再確認した
- 連続検索時に `内蔵サンプルデータ` へ落ちる挙動はやめ、サーバー側は空結果を返し、フロント側は前回結果を維持して再試行案内を出すようにした
- フロント側の検索は `AbortController` で前回リクエストを中断し、連打時に古い結果や誤ったフォールバックが混ざらないようにした
- `現在地から検索` タブを実装し、位置情報取得 → `/api/venues?lat&lng&range&count` → ハバーサイン距離計算 → 距離順表示のフローを追加した
- GPS検索では `500m / 1km / 2km` の距離フィルタを UI 側でかけ、上位3件を `店名 / ジャンル / 徒歩約N分（Nm） / 予算 / 営業時間 / 外部リンク` で描画するようにした
- `/api/venues` は `lat / lng / range / count` を受けられるようにし、ホットペッパー返却オブジェクトに `name / lat / lng / genre / budget / access / urls / open` を含めるようにした
- README に現在地検索の利用条件と HTTPS 起動手順を追記した
- 2026-03-30 時点の区切りとして、駅検索はホットペッパー駅名キーワード + 店舗トップページ補正で動作中
- 再開時の前提:
  公開URLは `https://161.33.151.114/`
  ローカルAPIは `127.0.0.1:8123`
  実行時は `HOTPEPPER_API_KEY` を付けて `server.py` を起動する
- GitHub `origin` は `https://github.com/tukinowa777/afterparty.git`
- 最新コミットは GitHub `master` まで push 済み
- 未追跡ファイルとして `AGENTS.md`、`certs/`、`tmp/` があるが、現時点ではコミット対象外

## 次にやること

- 実店舗データの取得元を決めて API 差し替えを行う
- 店の営業日・営業時間をより現実的なデータモデルへ拡張する
- HTTPS 配信またはデプロイ方法を用意してスマホの位置情報取得を安定させる
- 結果カードに予約導線や共有時の説明文を追加する
- 現在地許可が失敗したときの案内文を端末別に出し分ける
- ホットペッパーで0件の駅に対して補助データ源を使うか、問い合わせ条件をさらに見直す
